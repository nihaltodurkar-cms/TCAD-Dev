@echo off
rem Dev launcher generated by desktop/CMakeLists.txt: puts the tcad-gui
rem env's Qt/VTK DLLs on PATH (dev builds copy no DLLs), then runs the app.
set "PATH=@TCAD_RUNTIME_BIN_NATIVE@;%PATH%"
"%~dp0tcad_desktop.exe" %*
